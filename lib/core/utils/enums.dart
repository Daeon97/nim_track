// ignore_for_file: public_member_api_docs

enum Brightness {
  light,
  dark,
}

enum SeedColor {
  blue,
  yellow,
  purple,
  orange,
}

enum CardType {
  batteryLevel,
  time,
}

enum Field {
  name,
  batteryLevel,
  hash,
  latLng,
  timestamp,
}
